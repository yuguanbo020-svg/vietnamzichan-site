DATE=2026-08-29
AI_WORKER=Claude
PROJECT=VietnamZiChan (repo)
BRANCH=main
LAST_COMMIT=c6d15e6
COMPLETED=
- Scope decision: VietnamZiChan ALREADY has a working, independently-built multi-language system (vi/zh/en, generated by scripts/build_portal.py + scripts/generate_site.py, with real per-locale hreflang tags and a static top-bar switcher). Per the standing rule "不重复重构已经正常工作的东西", this pass did NOT replace or wrap that system with the SoulEntropy/VNGO shared i18n.js runtime — doing so would duplicate a working mechanism. Instead it added the ONE piece of Carbon's language requirements (item D) that was genuinely missing: persistence of the visitor's manual language choice, and a real (non-forced) first-visit recommendation.
- Patched scripts/build_portal.py (the actual source generator — NOT the generated output files, which get overwritten on every Netlify build) in two places:
  1. The root index.html generator: previously a hardcoded `<meta http-equiv="refresh" content="0;url=/vi/">` that sent EVERY visitor to /vi/ regardless of language, with no way to remember a returning visitor's choice. Replaced with a small inline script that checks localStorage/cookie first (manual/previous choice always wins, persists indefinitely) and falls back to a `navigator.languages`-based recommendation (zh/en/vi) only when nothing is stored — matching item D's explicit requirement and its literal example ("中国人在越南也可能继续需要中文"). A `<noscript>` fallback keeps the old unconditional redirect to /vi/ for non-JS clients.
  2. The shared JS bundle (assets/site.js, generated from the JS string constant in build_portal.py): added a small click-listener on the existing `.lang a` switcher links (present in the header of every generated page via the shared page template) that stores the clicked language to localStorage + a 365-day cookie. No page template/markup changes were needed — every page already loads assets/site.js.
- Re-ran `python3 scripts/build_portal.py` locally to regenerate index.html and assets/site.js from the patched source and confirm the change takes effect; git diff after regeneration touched exactly those 2 files plus scripts/build_portal.py itself — no unrelated drift in the ~150+ other generated pages (categories/cities/listings/etc.), confirming the generator is otherwise deterministic and untouched.
- Backed up the pre-patch script to scripts/build_portal.py.pre-i18n-backup before editing.
- Incidental fix (not the goal, but confirmed safe): the previously-committed index.html had a malformed `<body` tag with a duplicated, mis-closed paragraph — traceable to an earlier commit (75b1b37) that hand-edited the generated HTML file directly instead of the generator source, so the two drifted. Regenerating from the (already-correct) generator source produces clean, single-instance markup; content-wise nothing was lost — the intended sentence from that commit is still present, generated from source as it always should have been.
TESTED=
- `python3 -m py_compile scripts/build_portal.py` — syntax OK.
- Ran `python3 scripts/build_portal.py` — completed with `{"status":"ok","portal":"rebuilt"}`, no errors.
- git diff scoped to exactly index.html + assets/site.js + scripts/build_portal.py — no unintended regeneration drift elsewhere.
- Unit-tested the redirect-decision and click-extraction logic in isolation (Node): a stored/previous manual choice always wins over the current browser locale (verified with the exact "Chinese user in Vietnam" and "English-chooser now browsing in vi-VN" cases from item D); an unmatched browser locale falls back to the site's existing default (vi); the click-listener's href-to-locale regex correctly matches /zh/, /vi/, /en/ and correctly ignores non-language links like /news/.
NOT_TESTED=
- Full browser round-trip (open page → click switcher → reload → confirm redirect) — verified at the unit-logic level only, not with a live browser against the deployed site, due to time budget for this P2 pass. Low risk: the logic is a small, isolated, easily-reviewed snippet with no dependency on anything else on the page.
- `scripts/generate_site.py --languages zh` could NOT be run locally in this environment — it fails with a Python SyntaxError ("f-string expression part cannot include a backslash") on this machine's local python3 (3.10.12). This script was NOT modified by this pass. The error is consistent with an f-string syntax rule that Python 3.12 relaxed but 3.10/3.11 enforce — almost certainly an environment difference between this local shell's Python and whatever Python version Netlify's build actually uses (the zh/ pages already exist and are clearly built successfully in production, which wouldn't be possible if this script never ran). Flagging for awareness only, not treating it as a bug introduced by or discovered as blocking for this pass.
- Extending locale coverage to zh-TW/ko/ja (the remaining items in the user's VietnamZiChan priority list) was NOT attempted this pass — generate_site.py's LANGUAGES dict only defines zh/vi/en; adding new locales means extending that generator's content pipeline (translation source, per-locale UI dict, etc.), a larger and separate piece of work deferred to I18N_TRANSLATION_QUEUE.json / a future pass, consistent with "不同时把所有优先级改到一半".
BLOCKERS=
- Same as VNGO: GitHub push not attempted this pass (item H) — repo kept clean and fast-forwardable for the next AI/heartbeat with network access.
FILES_CHANGED=
scripts/build_portal.py (2 targeted edits: root index.html generator, JS bundle), scripts/build_portal.py.pre-i18n-backup (new, safety backup), index.html (regenerated), assets/site.js (regenerated), CARBON_HANDOFF.md (this file).
DATABASE_CHANGES=none
ROLLBACK=
Restore scripts/build_portal.py from scripts/build_portal.py.pre-i18n-backup (or `git checkout origin/main -- scripts/build_portal.py`), then re-run `python3 scripts/build_portal.py` to regenerate index.html/assets/site.js back to their prior state.
NEXT_ACTION=
1. Add zh-TW, ko, ja support to scripts/generate_site.py's LANGUAGES dict + a translation source for those locales, to reach the user's full VietnamZiChan priority list (vi/zh-CN/zh-TW/en/ko/ja) — a generator-level content task, good fit for OpenClaw/Qwen bulk translation once the framework/schema is agreed (see I18N_TRANSLATION_QUEUE.json).
2. Live browser verification of the persistence/redirect logic against the deployed Netlify preview once pushed.
3. Investigate the generate_site.py Python-version sensitivity as a low-priority hygiene item (confirm Netlify's actual Python version so a future environment change doesn't silently break zh/ builds) — not urgent, not part of this pass's scope.
SAFE_TO_CONTINUE=yes
